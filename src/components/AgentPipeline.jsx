const agents = [['01', 'Fetcher', 'Retrieves page', 'fetcher'], ['02', 'Differ', 'Detects changes', 'differ'], ['03', 'Analyst', 'AI assessment', 'analyst'], ['04', 'Notifier', 'Sends alert', 'notifier']];
const symbols = { IDLE: '○', RUNNING: '◌', COMPLETE: '✓', ERROR: '×' };
const styles = { IDLE: 'border-agent-idle text-text-secondary', RUNNING: 'border-agent-running text-blue-300 animate-pulse', COMPLETE: 'border-agent-complete text-green-400', ERROR: 'border-agent-error text-red-400' };
export default function AgentPipeline({ statuses }) { return <section><p className="panel-label">Agent pipeline status</p><div className="mt-3 flex flex-col gap-2 lg:flex-row lg:items-center">{agents.map(([number, name, role, key], index) => <div className="contents" key={key}><article className={`min-w-0 flex-1 border bg-surface px-3 py-3 transition-all ${styles[statuses[key]]}`}><div className="flex items-center justify-between text-xs"><span>AGENT {number}</span><span className={statuses[key] === 'RUNNING' ? 'animate-spin' : ''}>{symbols[statuses[key]]}</span></div><h3 className="mt-3 font-semibold text-text-primary">{name}</h3><p className="mt-1 text-xs text-text-secondary">{statuses[key] === 'RUNNING' ? 'Running…' : statuses[key] === 'COMPLETE' ? 'Complete' : statuses[key] === 'ERROR' ? 'Error' : role}</p></article>{index < agents.length - 1 && <span className={`hidden text-center text-lg transition-colors duration-500 lg:block ${statuses[key] === 'COMPLETE' && statuses[agents[index + 1][3]] === 'COMPLETE' ? 'animate-pulse text-green-500' : 'text-agent-idle'}`}>→</span>}</div>)}</div></section>; }
