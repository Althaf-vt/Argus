const colors = { LOW: 'border-severity-low bg-green-950/30 text-green-400', MEDIUM: 'border-severity-medium bg-amber-950/30 text-amber-400', HIGH: 'border-severity-high bg-red-950/30 text-red-400', CRITICAL: 'border-severity-critical bg-violet-950/40 text-violet-300' };
export default function ImpactPanel({ analysis }) { if (!analysis) return <section className="panel"><p className="panel-label">Impact analysis</p><div className="flex min-h-52 items-center justify-center border border-dashed border-border text-center text-sm text-text-secondary">Run a check to see AI impact analysis</div></section>; return <section className="panel animate-[fadeIn_.35s_ease-out]"><div className="flex items-center justify-between gap-4"><p className="panel-label">Impact analysis</p><span className={`border px-2 py-1 text-xs font-semibold tracking-wider ${colors[analysis.severity] || colors.LOW} ${analysis.severity === 'CRITICAL' ? 'animate-pulse' : ''}`}>{analysis.severity}</span></div><h2 className="mt-4 text-lg leading-relaxed text-text-primary">{analysis.summary}</h2><p className="mt-3 text-sm leading-6 text-text-secondary">{analysis.reasoning}</p><div className="mt-5"><p className="panel-label">Areas affected</p><div className="mt-2 flex flex-wrap gap-2">{analysis.areas_affected.map((area) => <span className="border border-border-strong bg-surface-2 px-2 py-1 text-xs text-text-secondary" key={area}>{area}</span>)}</div></div><div className="mt-5 border-l-2 border-agent-running bg-blue-950/20 p-3"><p className="panel-label">Recommended action</p><p className="mt-1 text-sm text-blue-100">{analysis.recommended_action}</p></div></section>; }
