export default function EntireWebsiteInventory({ target, message }) {
  const summary = target.discoverySummary || {};
  return <><p className="border border-border bg-surface px-3 py-2 text-sm text-text-secondary">{message}</p><section className="panel"><p className="panel-label">Initial discovery inventory · {target.status}</p><div className="mt-3 grid grid-cols-2 gap-2 text-sm text-text-secondary sm:grid-cols-3"><span>Found {summary.found ?? 0}</span><span>Eligible {summary.eligible ?? 0}</span><span>Active {summary.active ?? 0}</span><span>Ignored {summary.ignored ?? 0}</span><span>Failed {summary.failed ?? 0}</span><span>{summary.capped ? 'Budget capped' : 'Within budget'}</span></div><div className="mt-4 max-h-80 overflow-auto border border-border"><table className="w-full text-left text-xs"><thead className="bg-bg text-text-secondary"><tr><th className="p-2">Page</th><th className="p-2">State</th><th className="p-2">Source</th></tr></thead><tbody>{(target.inventory || []).map((page) => <tr key={page.id} className="border-t border-border"><td className="max-w-md truncate p-2">{page.url}</td><td className="p-2">{page.lifecycle}</td><td className="p-2">{page.source || '—'}</td></tr>)}</tbody></table></div></section></>;
}
