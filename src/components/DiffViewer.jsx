export default function DiffViewer({ diff }) { return <section className="panel"><div className="flex items-center justify-between"><p className="panel-label">Visual diff</p>{diff && <span className="text-xs text-text-secondary">+{diff.addedCount} / −{diff.removedCount} characters</span>}</div>{!diff ? <p className="mt-4 text-sm text-text-secondary">Changes will appear here after a monitored page updates.</p> : <pre className="mt-4 max-h-72 overflow-auto whitespace-pre-wrap border border-border bg-bg p-3 text-xs leading-5">{(diff.displayDiffs || diff.diffs).map(([op, text], index) => <span className={op === 1 ? 'bg-green-900/50 text-green-300' : op === -1 ? 'bg-red-950/60 text-red-300 line-through decoration-red-500/60' : 'text-text-secondary'} key={index}>{text}</span>)}</pre>}</section>; }
